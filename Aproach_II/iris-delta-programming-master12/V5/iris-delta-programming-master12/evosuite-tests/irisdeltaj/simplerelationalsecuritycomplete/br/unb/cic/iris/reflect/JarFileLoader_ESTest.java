/*
 * This file was automatically generated by EvoSuite
 * Sun Aug 31 20:22:07 GMT 2025
 */

package irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.reflect;

import org.junit.Test;
import org.junit.Before;
import static org.junit.Assert.*;
import static org.evosuite.runtime.EvoAssertions.*;
import irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.reflect.JarFileLoader;
import java.net.MalformedURLException;
import java.net.URL;

import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.junit.runner.RunWith;

@RunWith(EvoRunner.class) @EvoRunnerParameters(mockJVMNonDeterminism = true, useVFS = true, useVNET = true, resetStaticState = true, separateClassLoader = true) 
public class JarFileLoader_ESTest extends JarFileLoader_ESTest_scaffolding {

  /** تاخیر نیم‌ثانیه‌ای پیش از اجرای هر تست */
  @Before
  public void delay() throws Exception {
      Thread.sleep(500);
  }

  @Test(timeout = 4000)
  public void test0()  throws Throwable  {
      URL[] uRLArray0 = new URL[1];
      JarFileLoader jarFileLoader0 = new JarFileLoader(uRLArray0);
      try { 
        jarFileLoader0.addFile("jar:file://");
        fail("Expecting exception: MalformedURLException");
      
      } catch(MalformedURLException e) {
         //
         // invalid url: file://jar:file://!/ (java.net.MalformedURLException: For input string: \"file:\")
         //
         verifyException("java.net.URL", e);
      }
  }

  @Test(timeout = 4000)
  public void test1()  throws Throwable  {
      URL[] uRLArray0 = new URL[1];
      JarFileLoader jarFileLoader0 = new JarFileLoader(uRLArray0);
      jarFileLoader0.addFile("l;JArQ;'.v*JF_");
  }

  @Test(timeout = 4000)
  public void test00_constructorWithEmptyArray_hasNoUrls() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new java.net.URL[0]);
      assertEquals(0, loader.getURLs().length);
  }

  @Test(timeout = 4000)
  public void test01_constructorWithPreloadedUrl_keepsIt() throws Throwable {
      java.net.URL base = new java.net.URL("file:/");
      JarFileLoader loader = new JarFileLoader(new java.net.URL[]{ base });
      assertEquals(1, loader.getURLs().length);
      assertEquals(base, loader.getURLs()[0]);
  }

  @Test(timeout = 4000)
  public void test02_addFile_null_throwsNPE() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new java.net.URL[0]);
      try {
          loader.addFile((String) null);
          fail("Expecting exception: NullPointerException");
      } catch (NullPointerException e) {
          // ok
      }
  }

  @Test(timeout = 4000)
  public void test03_addFile_malformedString_throwsMalformed() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new java.net.URL[0]);
      try {
          loader.addFile("://definitely_bad");
          fail("Expecting exception: java.net.MalformedURLException");
      } catch (java.net.MalformedURLException e) {
          // ok
      }
  }

  @Test(timeout = 4000)
  public void test04_addFile_windowsBackslashes_throwsMalformed() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new java.net.URL[0]);
      try {
          loader.addFile("C:\\path with space\\fake.jar");
          fail("Expecting exception: java.net.MalformedURLException");
      } catch (java.net.MalformedURLException e) {
          // ok: بک‌اسلش و فاصله بدون کدگذاری باید خطا بدهد
      }
  }

  @Test(timeout = 4000)
  public void test05_addFile_unixLikePath_addsOneUrl() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new java.net.URL[0]);
      int before = loader.getURLs().length;
      // مسیر یونیکسی/سازگار که نیازی به وجود واقعی فایل ندارد
      loader.addFile("/tmp/fake.jar");
      assertEquals(before + 1, loader.getURLs().length);
      assertEquals("jar", loader.getURLs()[before].getProtocol());
  }

  // -----------------------
  // تست‌های اضافه‌شده
  // -----------------------

  @Test(timeout = 4000)
  public void test06_addFile_fileUrl_addsOneUrl() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new URL[0]);
      int before = loader.getURLs().length;
      loader.addFile("file:/tmp/f2.jar");
      assertEquals(before + 1, loader.getURLs().length);
      String u = loader.getURLs()[before].toString();
      assertTrue(u.startsWith("jar:file:/"));
      assertTrue(u.contains(".jar!/"));
  }

  @Test(timeout = 4000)
  public void test07_addFile_unixPath_withSpace_encodesSpace() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new URL[0]);
      int before = loader.getURLs().length;
      loader.addFile("/tmp/my file.jar");
      String u = loader.getURLs()[before].toString();
      assertTrue(u.contains("%20"));
      assertTrue(u.contains(".jar!/"));
  }

  @Test(timeout = 4000)
  public void test08_addFile_relativePath_addsUrl() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new URL[0]);
      int before = loader.getURLs().length;
      loader.addFile("relative-test.jar");
      assertEquals(before + 1, loader.getURLs().length);
      assertEquals("jar", loader.getURLs()[before].getProtocol());
      assertTrue(loader.getURLs()[before].toString().endsWith("!/"));
  }

  @Test(timeout = 4000)
  public void test09_addFile_samePathTwice_increasesCountByTwo() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new URL[0]);
      int before = loader.getURLs().length;
      loader.addFile("/tmp/dup.jar");
      loader.addFile("/tmp/dup.jar");
      assertEquals(before + 2, loader.getURLs().length);
  }

  @Test(timeout = 4000)
  public void test10_addFile_unicodePath_addsUrl() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new URL[0]);
      int before = loader.getURLs().length;
      loader.addFile("/tmp/کتابچه.jar");
      String u = loader.getURLs()[before].toString();
      assertTrue(u.startsWith("jar:file:/"));
      assertTrue(u.contains(".jar!/"));
  }

  @Test(timeout = 4000)
  public void test11_getURLs_arrayIndependence_afterMutation() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new URL[0]);
      loader.addFile("/tmp/one.jar");
      URL[] arr = loader.getURLs();
      String first = arr[0].toString();
      // تغییر آرایهٔ بازگشتی نباید وضعیت داخلی را تغییر دهد
      arr[0] = new URL("file:/");
      assertEquals(first, loader.getURLs()[0].toString());
  }

  @Test(timeout = 4000)
  public void test12_constructor_nullArray_throwsNPE() throws Throwable {
      try {
          new JarFileLoader(null);
          fail("Expecting NullPointerException");
      } catch (NullPointerException expected) {
          // ok
      }
  }

  @Test(timeout = 4000)
  public void test13_addFile_withJarSchemeString_throwsMalformed() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new URL[0]);
      try {
          loader.addFile("jar:file:/tmp/foo.jar");
          fail("Expecting MalformedURLException for jar-prefixed input");
      } catch (MalformedURLException expected) {
          // ok
      }
  }

  @Test(timeout = 4000)
  public void test14_addFile_pathWithHash_isEncoded() throws Throwable {
      JarFileLoader loader = new JarFileLoader(new URL[0]);
      int before = loader.getURLs().length;
      loader.addFile("/tmp/a#b.jar");
      String u = loader.getURLs()[before].toString();
      // # باید percent-encode شود
      assertTrue(u.contains("%23"));
      assertTrue(u.contains(".jar!/"));
  }

}
