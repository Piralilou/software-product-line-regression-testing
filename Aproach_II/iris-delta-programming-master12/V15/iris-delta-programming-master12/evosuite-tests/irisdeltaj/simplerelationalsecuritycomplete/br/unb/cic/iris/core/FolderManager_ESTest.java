/*
 * This file was automatically generated by EvoSuite
 * Sat Aug 30 22:49:28 GMT 2025
 */

package irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core;

import org.junit.Test;
import static org.junit.Assert.*;
import static org.evosuite.runtime.EvoAssertions.*;
import irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.FolderManager;
import irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.model.EmailMessage;
import java.util.List;
import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.junit.runner.RunWith;

@RunWith(EvoRunner.class) @EvoRunnerParameters(mockJVMNonDeterminism = true, useVFS = true, useVNET = true, resetStaticState = true, separateClassLoader = true)
public class FolderManager_ESTest extends FolderManager_ESTest_scaffolding {

  /** تاخیر نیم‌ثانیه‌ای برای هر تست */
  private void delay() {
      try { Thread.sleep(500L); } catch (InterruptedException e) { Thread.currentThread().interrupt(); }
  }

  @Test(timeout = 4000)
  public void test0()  throws Throwable  {
      delay();
      FolderManager folderManager0 = FolderManager.instance();
      // Undeclared exception!
      try {
        folderManager0.changeToFolder("");
        fail("Expecting exception: NoClassDefFoundError");

      } catch(NoClassDefFoundError e) {
         //
         // irisdeltaj/simplerelational/br/unb/cic/iris/persistence/relational/FolderDAO
         //
         verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
  }

  @Test(timeout = 4000)
  public void test1()  throws Throwable  {
      delay();
      FolderManager folderManager0 = FolderManager.instance();
      List<EmailMessage> list0 = folderManager0.listFolderMessages();
      assertTrue(list0.isEmpty());
  }

  @Test(timeout = 4000)
  public void test2()  throws Throwable  {
      delay();
      FolderManager folderManager0 = FolderManager.instance();
      String string0 = folderManager0.getCurrentFolderName();
      assertEquals("ROOT", string0);
  }

  @Test(timeout = 4000)
  public void test3()  throws Throwable  {
      delay();
      FolderManager folderManager0 = FolderManager.instance();
      // Undeclared exception!
      try {
        folderManager0.getMessage("(],`");
        fail("Expecting exception: NoClassDefFoundError");

      } catch(NoClassDefFoundError e) {
         //
         // irisdeltaj/simplerelational/br/unb/cic/iris/persistence/relational/EmailDAO
         //
         verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
  }

  @Test(timeout = 4000)
  public void test4()  throws Throwable  {
      delay();
      FolderManager folderManager0 = FolderManager.instance();
      // Undeclared exception!
      try {
        folderManager0.listFolders();
        fail("Expecting exception: NoClassDefFoundError");

      } catch(NoClassDefFoundError e) {
         //
         // irisdeltaj/simplerelational/br/unb/cic/iris/persistence/relational/FolderDAO
         //
         verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
  }

  @Test(timeout = 4000)
  public void test5()  throws Throwable  {
      delay();
      FolderManager folderManager0 = FolderManager.instance();
      List<EmailMessage> list0 = folderManager0.getCurrentMessages();
      assertNull(list0);
  }

  @Test(timeout = 4000)
  public void test6_singletonIdentity() throws Throwable {
      delay();
      FolderManager a = FolderManager.instance();
      FolderManager b = FolderManager.instance();
      assertSame(a, b);
  }

  @Test(timeout = 4000)
  public void test7_listFolderMessages_doesNotSetCurrentMessages_whenFolderIdIsNull() throws Throwable {
      delay();
      FolderManager mgr = FolderManager.instance();
      // با فولدر پیش‌فرض ROOT که id ندارد:
      List<EmailMessage> msgs = mgr.listFolderMessages();
      assertTrue(msgs.isEmpty());
      // currentMessages هنوز null می‌ماند چون شاخه else اجرا نشد
      assertNull(mgr.getCurrentMessages());
  }

  @Test(timeout = 4000)
  public void test8_getMessage_nullId_throwsNoClassDefFoundError() throws Throwable {
      delay();
      FolderManager mgr = FolderManager.instance();
      try {
          mgr.getMessage((String) null);
          fail("Expecting exception: NoClassDefFoundError");
      } catch (NoClassDefFoundError e) {
          // irisdeltaj/simplerelational/br/unb/cic/iris/persistence/relational/EmailDAO
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
  }

  @Test(timeout = 4000)
  public void test9_changeToFolder_nullName_throwsNoClassDefFoundError() throws Throwable {
      delay();
      FolderManager mgr = FolderManager.instance();
      try {
          mgr.changeToFolder((String) null);
          fail("Expecting exception: NoClassDefFoundError");
      } catch (NoClassDefFoundError e) {
          // irisdeltaj/simplerelational/br/unb/cic/iris/persistence/relational/FolderDAO
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
  }

  @Test(timeout = 4000)
  public void test10_getCurrentFolderName_afterListFolderMessages_stillRoot() throws Throwable {
      delay();
      FolderManager mgr = FolderManager.instance();
      mgr.listFolderMessages(); // برای فولدر پیش‌فرض کاری نمی‌کند
      assertEquals("ROOT", mgr.getCurrentFolderName());
  }

  // ======================= تست‌های اضافه‌شده =======================

  @Test(timeout = 4000)
  public void test11_getCurrentMessages_staysNull_afterListFoldersThrows() throws Throwable {
      delay();
      FolderManager mgr = FolderManager.instance();
      try {
          mgr.listFolders();
          fail("Expecting exception: NoClassDefFoundError");
      } catch (NoClassDefFoundError e) {
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
      // پس از خطا، وضعیت نباید تغییر کند
      assertNull(mgr.getCurrentMessages());
      assertEquals("ROOT", mgr.getCurrentFolderName());
  }

  @Test(timeout = 4000)
  public void test12_listFolderMessages_twice_emptyAndFolderUnchanged() throws Throwable {
      delay();
      FolderManager mgr = FolderManager.instance();
      List<EmailMessage> first = mgr.listFolderMessages();
      List<EmailMessage> second = mgr.listFolderMessages();
      assertNotNull(first);
      assertNotNull(second);
      assertTrue(first.isEmpty());
      assertTrue(second.isEmpty());
      assertEquals("ROOT", mgr.getCurrentFolderName());
  }

  @Test(timeout = 4000)
  public void test13_changeToFolder_spaces_throwsNoClassDefFoundError() throws Throwable {
      delay();
      FolderManager mgr = FolderManager.instance();
      try {
          mgr.changeToFolder("   ");
          fail("Expecting exception: NoClassDefFoundError");
      } catch (NoClassDefFoundError e) {
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
  }

  @Test(timeout = 4000)
  public void test14_getMessage_emptyString_throwsNoClassDefFoundError() throws Throwable {
      delay();
      FolderManager mgr = FolderManager.instance();
      try {
          mgr.getMessage("");
          fail("Expecting exception: NoClassDefFoundError");
      } catch (NoClassDefFoundError e) {
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
  }

  @Test(timeout = 4000)
  public void test15_instance_multipleCalls_sameReference() throws Throwable {
      delay();
      FolderManager ref = FolderManager.instance();
      for (int i = 0; i < 5; i++) {
          assertSame(ref, FolderManager.instance());
      }
  }

  @Test(timeout = 4000)
  public void test16_stateRemainsRoot_afterFailedGetMessage() throws Throwable {
      delay();
      FolderManager mgr = FolderManager.instance();
      try {
          mgr.getMessage("non-existent-id");
          fail("Expecting exception: NoClassDefFoundError");
      } catch (NoClassDefFoundError e) {
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
      assertEquals("ROOT", mgr.getCurrentFolderName());
      // currentMessages همچنان نباید مقداردهی شده باشد
      assertNull(mgr.getCurrentMessages());
  }
}
