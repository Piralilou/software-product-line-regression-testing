/*
 * This file was automatically generated by EvoSuite
 * Fri Aug 29 23:01:11 GMT 2025
 */

package irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.command;

import org.junit.Test;
import org.junit.Before;
import java.io.ByteArrayOutputStream;
import java.io.PrintStream;
import java.nio.charset.StandardCharsets;
import java.util.ArrayList;
import java.util.Date;

import static org.junit.Assert.*;
import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.junit.runner.RunWith;

@RunWith(EvoRunner.class) 
@EvoRunnerParameters(
    mockJVMNonDeterminism = true, 
    useVFS = true, 
    useVNET = true, 
    resetStaticState = true, 
    separateClassLoader = true
) 
public class AbstractListMessagesCommand_ESTest extends AbstractListMessagesCommand_ESTest_scaffolding {

  // تاخیر نیم‌ثانیه‌ای برای هر تست (با timeout=4000 ناسازگار نمی‌شود)
  @Before
  public void halfSecondDelay() throws Exception {
      Thread.sleep(500L);
  }

  @Test
  public void notGeneratedAnyTest() {
    // EvoSuite did not generate any tests
  }

  /** زیرکلاس ساده برای تست کردن متد protectedِ print(...) */
  private static class DummyListMessagesCommand extends irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.command.AbstractListMessagesCommand {
      @Override public void explain() { /* no-op */ }
      @Override public void handleExecute() { /* no-op */ }
      @Override public String getCommandName() { return "dummy"; }
      /** اکسپوز کردن print برای تست */
      public void callPrint(java.util.List<irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.model.EmailMessage> msgs) {
          print(msgs);
      }
      /** دسترسی به فرمت‌کننده برای تست فرمت تاریخ */
      public java.text.DateFormat getFormatter() { return this.formatter; }
  }

  /** هِلپر: گرفتن stdout با java.lang.System (برای جلوگیری از تداخل با EvoSuite System) */
  private String captureStdout(final Runnable r) {
      PrintStream orig = java.lang.System.out;
      ByteArrayOutputStream baos = new ByteArrayOutputStream();
      PrintStream ps = new PrintStream(baos);
      java.lang.System.setOut(ps);
      try {
          r.run();
      } finally {
          java.lang.System.setOut(orig);
          ps.flush();
          ps.close();
      }
      return new String(baos.toByteArray(), StandardCharsets.UTF_8);
  }

  @Test(timeout = 4000)
  public void test0_print_withEmptyList_printsNothing() throws Throwable {
      final DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      final java.util.ArrayList<irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.model.EmailMessage> empty =
              new java.util.ArrayList<irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.model.EmailMessage>();

      String out = captureStdout(new Runnable() { @Override public void run() { cmd.callPrint(empty); } });
      // لیست خالی → خروجی نباید داشته باشد
      assertTrue(out.trim().isEmpty());
      assertEquals("dummy", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test1_formatter_containsAt_whenFormattingEpoch() throws Throwable {
      DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      java.text.DateFormat df = cmd.getFormatter();
      // رشته‌ی الگو در کلاس: "dd/MMM/yy 'at' HH:mm" → باید 'at' در خروجی باشد
      String formatted = df.format(new Date(0L));
      assertTrue(formatted.contains("at"));
  }

  @Test(timeout = 4000)
  public void test2_print_withNullList_throwsNullPointer() throws Throwable {
      DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      try {
          cmd.callPrint(null);
          fail("Expecting NullPointerException");
      } catch (NullPointerException e) {
          // ok
      }
  }

  @Test(timeout = 4000)
  public void test3_print_listWithOneNullElement_throwsNullPointer() throws Throwable {
      DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      java.util.ArrayList<irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.model.EmailMessage> list =
              new java.util.ArrayList<irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.model.EmailMessage>();
      list.add(null);
      try {
          cmd.callPrint(list);
          fail("Expecting NullPointerException");
      } catch (NullPointerException e) {
          // ok: به خاطر msg.getDate() روی null
      }
  }

  @Test(timeout = 4000)
  public void test4_explainAndName_doNotCrash() throws Throwable {
      final DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      // explain پیاده‌سازیِ no-op است؛ ممکن است چیزی چاپ نکند
      assertNotNull(out);
      assertEquals("dummy", cmd.getCommandName());
  }

  // ==================== تست‌های اضافه‌شده ====================

  @Test(timeout = 4000)
  public void test5_formatter_sameInstant_twice_sameString() throws Throwable {
      DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      java.text.DateFormat df = cmd.getFormatter();
      Date t = new Date(65_432_100L); // یک لحظه‌ی دلخواه
      String s1 = df.format(t);
      String s2 = df.format(t);
      assertEquals(s1, s2);
  }

  @Test(timeout = 4000)
  public void test6_formatter_differentInstants_produceDifferentStrings() throws Throwable {
      DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      java.text.DateFormat df = cmd.getFormatter();
      String s1 = df.format(new Date(0L));
      String s2 = df.format(new Date(60_000L)); // +۱ دقیقه
      assertNotEquals(s1, s2);
  }

  @Test(timeout = 4000)
  public void test7_formatter_timeToken_containsColonAndLengthAtLeast5() throws Throwable {
      DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      java.text.DateFormat df = cmd.getFormatter();
      String formatted = df.format(new Date(0L));
      // انتظار داریم " ... at HH:mm" باشد → توکن بعد از " at " شامل ':' است
      int idx = formatted.indexOf(" at ");
      assertTrue("formatted should contain ' at ' segment", idx >= 0);
      String timePart = formatted.substring(idx + 4).trim();
      assertTrue(timePart.contains(":"));
      assertTrue(timePart.length() >= 4); // حداقل H:mm در برخی localeها
  }

  @Test(timeout = 4000)
  public void test8_formatter_isSimpleDateFormat_andPatternContainsAtIfAvailable() throws Throwable {
      DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      java.text.DateFormat df = cmd.getFormatter();
      if (df instanceof java.text.SimpleDateFormat) {
          String pattern = ((java.text.SimpleDateFormat) df).toPattern();
          assertTrue(pattern.contains("'at'"));
      } else {
          // اگر نوع دیگری بود، فقط مطمئن شویم شیء تهی نیست
          assertNotNull(df);
      }
  }

  @Test(timeout = 4000)
  public void test9_print_emptyList_calledTwice_noAccumulatedOutput() throws Throwable {
      final DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      final java.util.List<irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.model.EmailMessage> empty =
              new java.util.ArrayList<irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.model.EmailMessage>();

      String out1 = captureStdout(new Runnable() { @Override public void run() { cmd.callPrint(empty); } });
      String out2 = captureStdout(new Runnable() { @Override public void run() { cmd.callPrint(empty); } });

      assertTrue(out1.trim().isEmpty());
      assertTrue(out2.trim().isEmpty());
  }

  @Test(timeout = 4000)
  public void test10_multipleDummyInstances_sameFormatterPatternWhenSimpleDateFormat() throws Throwable {
      DummyListMessagesCommand a = new DummyListMessagesCommand();
      DummyListMessagesCommand b = new DummyListMessagesCommand();
      java.text.DateFormat dfa = a.getFormatter();
      java.text.DateFormat dfb = b.getFormatter();
      if (dfa instanceof java.text.SimpleDateFormat && dfb instanceof java.text.SimpleDateFormat) {
          assertEquals(((java.text.SimpleDateFormat) dfa).toPattern(),
                       ((java.text.SimpleDateFormat) dfb).toPattern());
      } else {
          // اگر یکی/هر دو SimpleDateFormat نبودند، حداقل هر دو ناتهی باشند
          assertNotNull(dfa);
          assertNotNull(dfb);
      }
  }

  @Test(timeout = 4000)
  public void test11_print_withLargePreallocatedEmptyList_noOutput() throws Throwable {
      final DummyListMessagesCommand cmd = new DummyListMessagesCommand();
      final java.util.List<irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.model.EmailMessage> bigEmpty =
              new java.util.ArrayList<irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.core.model.EmailMessage>(10_000);

      String out = captureStdout(new Runnable() { @Override public void run() { cmd.callPrint(bigEmpty); } });
      assertTrue(out.trim().isEmpty());
  }

  @Test(timeout = 4000)
  public void test12_captureStdout_restoresSystemOut() throws Throwable {
      String before = "ping";
      System.out.print(before);
      String captured = captureStdout(new Runnable() { @Override public void run() { System.out.print("inside"); } });
      // بعد از خروج از captureStdout باید همچنان بتوانیم روی System.out چاپ کنیم
      System.out.print(before);
      assertEquals("inside", captured);
  }
}
