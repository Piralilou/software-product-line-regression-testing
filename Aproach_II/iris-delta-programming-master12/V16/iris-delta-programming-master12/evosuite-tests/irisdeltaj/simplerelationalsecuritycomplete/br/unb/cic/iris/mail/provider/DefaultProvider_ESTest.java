/*
 * This file was automatically generated by EvoSuite
 * Sat Aug 30 23:06:58 GMT 2025
 */

package irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.mail.provider;

import org.junit.Test;
import static org.junit.Assert.*;
import static org.evosuite.runtime.EvoAssertions.*;
import irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.mail.provider.DefaultProvider;
import java.util.Properties;
import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.junit.runner.RunWith;

@RunWith(EvoRunner.class) @EvoRunnerParameters(mockJVMNonDeterminism = true, useVFS = true, useVNET = true, resetStaticState = true, separateClassLoader = true) 
public class DefaultProvider_ESTest extends DefaultProvider_ESTest_scaffolding {

  @Test(timeout = 4000)
  public void test0()  throws Throwable  {
      Thread.sleep(500);
      Properties properties0 = new Properties();
      properties0.put("provider.description", "");
      DefaultProvider defaultProvider0 = null;
      try {
        defaultProvider0 = new DefaultProvider(properties0);
        fail("Expecting exception: NullPointerException");
      
      } catch(NullPointerException e) {
         //
         // no message in exception (getMessage() returned null)
         //
         verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.mail.provider.DefaultProvider", e);
      }
  }

  private Properties fullProps(String auth) {
      Properties p = new Properties();
      // کلیدها مطابق با DefaultProvider.retrieveAndRemoveProperty(...)
      p.setProperty("provider.description", "desc");
      p.setProperty("provider.name", "MyMail");
      p.setProperty("provider.user", "user@example.com");
      p.setProperty("provider.password", "secret");
      p.setProperty("provider.store.protocol", "imap");
      p.setProperty("provider.store.host", "imap.example.com");
      p.setProperty("provider.store.port", "993");
      p.setProperty("provider.auth", auth);
      p.setProperty("provider.transport.protocol", "smtp");
      p.setProperty("provider.transport.host", "smtp.example.com");
      p.setProperty("provider.transport.port", "587");
      return p;
  }

  @Test(timeout = 4000)
  public void test10_parseAllAndStripKeys() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("TrUe"); // حساس به حروف نباشد
      p.setProperty("custom.key", "keep"); // باید در Properties داخلی بماند

      DefaultProvider dp = new DefaultProvider(p);

      // مقداردهی فیلدها
      assertEquals("MyMail", dp.getName());
      assertEquals("desc", dp.getDescription());
      assertEquals("user@example.com", dp.getUsername());
      assertEquals("secret", dp.getPassword());
      assertEquals("imap", dp.getStoreProtocol());
      assertEquals("imap.example.com", dp.getStoreHost());
      assertEquals(993, dp.getStorePort());
      assertEquals("smtp", dp.getTransportProtocol());
      assertEquals("smtp.example.com", dp.getTransportHost());
      assertEquals(587, dp.getTransportPort());
      assertTrue(dp.isAuthenticationEnabled());

      // Properties داخلی فقط باید مقادیر باقی‌مانده را داشته باشد
      Properties propsIn = dp.getProperties();
      assertEquals("keep", propsIn.getProperty("custom.key"));
      assertFalse(propsIn.containsKey("provider.name"));
      assertFalse(propsIn.containsKey("provider.transport.port"));

      // آبجکت properties داخلی جدید است (نه همان ورودی)
      assertNotSame(propsIn, p);

      // خود p هم بعد از parse باید فقط کلید سفارشی را داشته باشد
      assertTrue(p.containsKey("custom.key"));
      assertEquals(1, p.size());
  }

  @Test(timeout = 4000)
  public void test11_authDisabled() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("false");
      DefaultProvider dp = new DefaultProvider(p);
      assertFalse(dp.isAuthenticationEnabled());
  }

  @Test(timeout = 4000)
  public void test12_invalidStorePort_throwsNumberFormat() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("true");
      p.setProperty("provider.store.port", "oops");
      try {
          new DefaultProvider(p);
          fail("Expecting exception: NumberFormatException");
      } catch (NumberFormatException e) {
          // ok
      }
  }

  @Test(timeout = 4000)
  public void test13_invalidTransportPort_throwsNumberFormat() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("true");
      p.setProperty("provider.transport.port", "N/A");
      try {
          new DefaultProvider(p);
          fail("Expecting exception: NumberFormatException");
      } catch (NumberFormatException e) {
          // ok
      }
  }

  @Test(timeout = 4000)
  public void test14_missingRequiredKey_throwsNPE() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("true");
      p.remove("provider.name"); // retrieveAndRemoveProperty -> getProperty(...).trim() => NPE
      try {
          new DefaultProvider(p);
          fail("Expecting exception: NullPointerException");
      } catch (NullPointerException e) {
          // ok
      }
  }

  @Test(timeout = 4000)
  public void test15_trimsValues() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps(" true "); // auth با فاصله
      p.setProperty("provider.transport.host", "  smtp.example.com  ");
      p.setProperty("provider.store.host", "  imap.example.com  ");
      DefaultProvider dp = new DefaultProvider(p);
      assertEquals("smtp.example.com", dp.getTransportHost());
      assertEquals("imap.example.com", dp.getStoreHost());
      assertTrue(dp.isAuthenticationEnabled()); // " true " -> trim -> true
  }

  @Test(timeout = 4000)
  public void test16_cloneDelegatesToSuper_returnsNull() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("true");
      DefaultProvider dp = new DefaultProvider(p);
      assertNull(dp.clone()); // چون super.clone() در AbstractEmailProvider مقدار null می‌دهد
  }

  @Test(timeout = 4000)
  public void test17_usernameAndPasswordRoundtrip() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("true");
      DefaultProvider dp = new DefaultProvider(p);
      assertEquals("user@example.com", dp.getUsername());
      assertEquals("secret", dp.getPassword());
  }

  // ---------------------- تست‌های افزوده‌شده ----------------------

  @Test(timeout = 4000)
  public void test18_authCaseInsensitive_yes() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("YeS");
      DefaultProvider dp = new DefaultProvider(p);
      assertTrue(dp.isAuthenticationEnabled());
  }

  @Test(timeout = 4000)
  public void test19_preserveUnknownProperties_multiple() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("true");
      p.setProperty("mail.imap.ssl.enable", "true");
      p.setProperty("custom.flag", "1");
      DefaultProvider dp = new DefaultProvider(p);

      Properties in = dp.getProperties();
      assertEquals("true", in.getProperty("mail.imap.ssl.enable"));
      assertEquals("1", in.getProperty("custom.flag"));

      // ورودی پس از parse فقط باید کلیدهای ناشناخته را داشته باشد
      assertTrue(p.containsKey("mail.imap.ssl.enable"));
      assertTrue(p.containsKey("custom.flag"));
      assertEquals(2, p.size());
  }

  @Test(timeout = 4000)
  public void test20_constructor_nullProperties_throwsNPE() throws Throwable {
      Thread.sleep(500);
      try {
          new DefaultProvider(null);
          fail("Expecting NullPointerException");
      } catch (NullPointerException e) {
          // ok
      }
  }

  @Test(timeout = 4000)
  public void test21_blankAuth_meansDisabled() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("   ");
      DefaultProvider dp = new DefaultProvider(p);
      assertFalse(dp.isAuthenticationEnabled());
  }

  @Test(timeout = 4000)
  public void test22_portsParsedWithWhitespace() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("true");
      p.setProperty("provider.store.port", "  143  ");
      p.setProperty("provider.transport.port", "  25 ");
      DefaultProvider dp = new DefaultProvider(p);
      assertEquals(143, dp.getStorePort());
      assertEquals(25, dp.getTransportPort());
  }

  @Test(timeout = 4000)
  public void test23_emptyPassword_allowed() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("false");
      p.setProperty("provider.password", "");
      DefaultProvider dp = new DefaultProvider(p);
      assertEquals("", dp.getPassword());
      assertFalse(dp.isAuthenticationEnabled());
  }

  @Test(timeout = 4000)
  public void test24_trimNameAndDescription() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("true");
      p.setProperty("provider.name", "  Mail  ");
      p.setProperty("provider.description", "  Desc  ");
      DefaultProvider dp = new DefaultProvider(p);
      assertEquals("Mail", dp.getName());
      assertEquals("Desc", dp.getDescription());
  }

  @Test(timeout = 4000)
  public void test25_inputIsolation_mutatingOriginalAfterCtorDoesNotAffectDp() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("true");
      p.setProperty("left", "1");
      DefaultProvider dp = new DefaultProvider(p);

      // تغییر روی p بعد از ساخت
      p.setProperty("new.key", "x");

      // درون dp نباید دیده شود
      Properties in = dp.getProperties();
      assertNull(in.getProperty("new.key"));

      // اما کلید باقی‌مانده اولیه باید باشد
      assertEquals("1", in.getProperty("left"));
  }

  @Test(timeout = 4000)
  public void test26_mutatingReturnedPropsDoesNotAffectInput() throws Throwable {
      Thread.sleep(500);
      Properties p = fullProps("true");
      p.setProperty("only", "ok");
      DefaultProvider dp = new DefaultProvider(p);

      Properties in = dp.getProperties();
      in.setProperty("x", "1"); // تغییر روی Properties داخلی

      // ورودی نباید تحت تاثیر قرار گیرد
      assertTrue(p.containsKey("only"));
      assertFalse(p.containsKey("x"));
  }
}
