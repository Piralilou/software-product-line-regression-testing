/*
 * This file was automatically generated by EvoSuite
 * Sat Aug 30 23:04:05 GMT 2025
 */

package irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.mail.provider;

import org.junit.Test;
import static org.junit.Assert.*;
import static org.evosuite.runtime.EvoAssertions.*;
import irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.mail.provider.DefaultProvider;
import java.util.Properties;
import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.junit.runner.RunWith;

@RunWith(EvoRunner.class) @EvoRunnerParameters(mockJVMNonDeterminism = true, useVFS = true, useVNET = true, resetStaticState = true, separateClassLoader = true)
public class AbstractEmailProvider_ESTest extends AbstractEmailProvider_ESTest_scaffolding {

  /** تاخیر نیم‌ثانیه‌ای برای هر تست */
  private void delay() {
      try { Thread.sleep(500L); } catch (InterruptedException e) { Thread.currentThread().interrupt(); }
  }

  @Test(timeout = 4000)
  public void test0()  throws Throwable  {
      delay();
      Properties properties0 = new Properties();
      properties0.put("provider.description", "provider.description");
      DefaultProvider defaultProvider0 = null;
      try {
        defaultProvider0 = new DefaultProvider(properties0);
        fail("Expecting exception: NullPointerException");

      } catch(NullPointerException e) {
         //
         // no message in exception (getMessage() returned null)
         //
         verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.mail.provider.DefaultProvider", e);
      }
  }

  /** یک زیرکلاس خیلی ساده برای تستِ کلاس انتزاعی */
  private static class DummyProvider extends AbstractEmailProvider { }

  @Test(timeout = 4000)
  public void test10_defaultValues() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      // مقادیر پیش‌فرض
      assertNull(p.getName());
      assertNull(p.getDescription());
      assertNull(p.getTransportProtocol());
      assertNull(p.getStoreProtocol());
      assertNull(p.getTransportHost());
      assertNull(p.getStoreHost());
      assertNull(p.getUsername());
      assertNull(p.getPassword());
      assertEquals(0, p.getTransportPort());
      assertEquals(0, p.getStorePort());
      assertTrue(p.isAuthenticationEnabled());
      assertNotNull(p.getProperties());
      assertTrue(p.getProperties().isEmpty());
  }

  @Test(timeout = 4000)
  public void test11_settersAndGettersRoundTrip() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();

      // setXxx ها protected هستند اما چون این تست در همان package است قابل دسترسی‌اند
      p.setName("MyMail");
      p.setDescription("Desc");
      p.setTransportProtocol("smtp");
      p.setStoreProtocol("imap");
      p.setTransportHost("smtp.example.com");
      p.setStoreHost("imap.example.com");
      p.setTransportPort(587);
      p.setStorePort(993);
      p.setUsername("user@example.com");
      p.setPassword("secret");

      assertEquals("MyMail", p.getName());
      assertEquals("Desc", p.getDescription());
      assertEquals("smtp", p.getTransportProtocol());
      assertEquals("imap", p.getStoreProtocol());
      assertEquals("smtp.example.com", p.getTransportHost());
      assertEquals("imap.example.com", p.getStoreHost());
      assertEquals(587, p.getTransportPort());
      assertEquals(993, p.getStorePort());
      assertEquals("user@example.com", p.getUsername());
      assertEquals("secret", p.getPassword());
  }

  @Test(timeout = 4000)
  public void test12_propertiesReferenceAndMutation() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      Properties props = p.getProperties();
      props.put("k1", "v1");
      // همان رفرنس برگردانده می‌شود، نه کپی
      assertSame(props, p.getProperties());
      assertEquals("v1", p.getProperties().get("k1"));
  }

  @Test(timeout = 4000)
  public void test13_toggleAuthenticationFlag() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      assertTrue(p.isAuthenticationEnabled());
      p.setAuthenticationEnabled(false);
      assertFalse(p.isAuthenticationEnabled());
      p.setAuthenticationEnabled(true);
      assertTrue(p.isAuthenticationEnabled());
  }

  @Test(timeout = 4000)
  public void test14_cloneReturnsNull() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      // پیاده‌سازی فعلی clone مقدار null برمی‌گرداند
      assertNull(p.clone());
  }

  @Test(timeout = 4000)
  public void test15_hostsAndPortsIndependence() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      p.setTransportHost("smtp.local");
      p.setStoreHost("imap.local");
      p.setTransportPort(25);
      p.setStorePort(110);

      assertEquals("smtp.local", p.getTransportHost());
      assertEquals("imap.local", p.getStoreHost());
      assertEquals(25, p.getTransportPort());
      assertEquals(110, p.getStorePort());

      // تغییر فقط یکی نباید دیگری را عوض کند
      p.setTransportPort(2525);
      assertEquals(2525, p.getTransportPort());
      assertEquals(110, p.getStorePort());
  }

  // ======================= تست‌های اضافه‌شده =======================

  @Test(timeout = 4000)
  public void test16_setNullFields_allowedAndKeptNull() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      p.setName(null);
      p.setDescription(null);
      p.setTransportProtocol(null);
      p.setStoreProtocol(null);
      p.setTransportHost(null);
      p.setStoreHost(null);
      p.setUsername(null);
      p.setPassword(null);
      assertNull(p.getName());
      assertNull(p.getDescription());
      assertNull(p.getTransportProtocol());
      assertNull(p.getStoreProtocol());
      assertNull(p.getTransportHost());
      assertNull(p.getStoreHost());
      assertNull(p.getUsername());
      assertNull(p.getPassword());
  }

  @Test(timeout = 4000)
  public void test17_negativePortsAcceptedAsIs() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      p.setTransportPort(-1);
      p.setStorePort(-999);
      assertEquals(-1, p.getTransportPort());
      assertEquals(-999, p.getStorePort());
  }

  @Test(timeout = 4000)
  public void test18_emptyStringsPersist() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      p.setName("");
      p.setDescription(" ");
      p.setUsername("");
      p.setPassword("");
      assertEquals("", p.getName());
      assertEquals(" ", p.getDescription());
      assertEquals("", p.getUsername());
      assertEquals("", p.getPassword());
  }

  @Test(timeout = 4000)
  public void test19_properties_putAndRemove_reflected() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      Properties props = p.getProperties();
      props.put("a", "1");
      props.put("b", "2");
      assertEquals(2, p.getProperties().size());
      props.remove("a");
      assertFalse(p.getProperties().containsKey("a"));
      assertTrue(p.getProperties().containsKey("b"));
  }

  @Test(timeout = 4000)
  public void test20_protocolsIndependence() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      p.setTransportProtocol("smtp");
      p.setStoreProtocol("imap");
      assertEquals("smtp", p.getTransportProtocol());
      assertEquals("imap", p.getStoreProtocol());
      p.setStoreProtocol("pop3");
      assertEquals("smtp", p.getTransportProtocol());
      assertEquals("pop3", p.getStoreProtocol());
  }

  @Test(timeout = 4000)
  public void test21_clone_isNullAndIdempotent() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      assertNull(p.clone());
      assertNull(p.clone()); // دوباره هم باید null بدهد
  }

  @Test(timeout = 4000)
  public void test22_authFlagUnaffectedByPropertyMutations() throws Throwable {
      delay();
      DummyProvider p = new DummyProvider();
      assertTrue(p.isAuthenticationEnabled());
      p.getProperties().put("mail.smtp.auth", "false");
      // تغییر پراپرتی نباید فلگ داخلی را دستکاری کند
      assertTrue(p.isAuthenticationEnabled());
      p.setAuthenticationEnabled(false);
      assertFalse(p.isAuthenticationEnabled());
  }
}
