/*
 * This file was automatically generated by EvoSuite
 * Fri Aug 29 21:43:15 GMT 2025
 */

package irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.command.console;

import org.junit.Test;
import java.io.ByteArrayOutputStream;
import java.io.PrintStream;
import java.nio.charset.StandardCharsets;

import static org.junit.Assert.*;
import static org.evosuite.runtime.EvoAssertions.*;
import irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.command.console.ConsoleListFoldersCommand;
import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.junit.runner.RunWith;
import org.junit.Before;

@RunWith(EvoRunner.class)
@EvoRunnerParameters(
    mockJVMNonDeterminism = true,
    useVFS = true,
    useVNET = true,
    resetStaticState = true,
    separateClassLoader = true
)
public class ConsoleListFoldersCommand_ESTest extends ConsoleListFoldersCommand_ESTest_scaffolding {

  // --- Per-test delay: 0.5 second (500 ms), safe under @Test(timeout = 4000)
  @Before
  public void halfSecondDelay() throws Exception {
      Thread.sleep(500L);
  }

  @Test(timeout = 4000)
  public void test0()  throws Throwable  {
      ConsoleListFoldersCommand consoleListFoldersCommand0 = new ConsoleListFoldersCommand();
      // Undeclared exception!
      try { 
        consoleListFoldersCommand0.handleExecute();
        fail("Expecting exception: NoClassDefFoundError");
      } catch(NoClassDefFoundError e) {
         //
         // irisdeltaj/simplerelational/br/unb/cic/iris/persistence/relational/FolderDAO
         //
         verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
  }

  @Test(timeout = 4000)
  public void test1()  throws Throwable  {
      ConsoleListFoldersCommand consoleListFoldersCommand0 = new ConsoleListFoldersCommand();
      String string0 = consoleListFoldersCommand0.getCommandName();
      assertEquals("lf", string0);
  }

  @Test(timeout = 4000)
  public void test2()  throws Throwable  {
      ConsoleListFoldersCommand consoleListFoldersCommand0 = new ConsoleListFoldersCommand();
      consoleListFoldersCommand0.explain();
      assertEquals("lf", consoleListFoldersCommand0.getCommandName());
  }

  /** helper: گرفتن خروجی System.out بدون لامبدا (سازگار با Java 8) */
  private String captureStdout(final Runnable r) {
      PrintStream orig = System.out;
      ByteArrayOutputStream baos = new ByteArrayOutputStream();
      PrintStream ps = new PrintStream(baos);
      System.setOut(ps);
      try {
          r.run();
      } finally {
          System.setOut(orig);
          ps.flush();
          ps.close();
      }
      return new String(baos.toByteArray(), StandardCharsets.UTF_8);
  }

  @Test(timeout = 4000)
  public void test3_explain_printsUsageAndKey() throws Throwable {
      final ConsoleListFoldersCommand cmd = new ConsoleListFoldersCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      // فرمت کلی: (lf) - <پیام i18n>
      assertTrue(out.contains("(lf)"));
      // در نبود ResourceBundle، خود کلید i18n چاپ می‌شود:
      assertTrue(out.contains("command.list.folders.explain"));
      assertEquals("lf", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test4_explain_multipleCalls_consistent() throws Throwable {
      final ConsoleListFoldersCommand cmd = new ConsoleListFoldersCommand();
      String out1 = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      String out2 = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertTrue(out1.contains("(lf)"));
      assertTrue(out2.contains("(lf)"));
      assertTrue(out1.contains("command.list.folders.explain"));
      assertTrue(out2.contains("command.list.folders.explain"));
  }

  @Test(timeout = 4000)
  public void test5_handleExecute_noStdoutBeforeCrash_andThrowsFromFolderManager() throws Throwable {
      ConsoleListFoldersCommand cmd = new ConsoleListFoldersCommand();

      // دستی stdout را می‌گیریم تا همزمان بتوانیم Exception را هم بررسی کنیم
      PrintStream orig = System.out;
      ByteArrayOutputStream baos = new ByteArrayOutputStream();
      PrintStream ps = new PrintStream(baos);
      System.setOut(ps);
      Throwable thrown = null;
      try {
          cmd.handleExecute(); // به‌خاطر نبود FolderDAO باید کرش کند
      } catch (Throwable t) {
          thrown = t;
      } finally {
          System.setOut(orig);
          ps.flush();
          ps.close();
      }

      // باید استثناء رخ داده باشد و از سمت FolderManager باشد
      assertNotNull("Expected an error due to missing infrastructure", thrown);
      assertTrue(thrown instanceof NoClassDefFoundError);
      verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", (NoClassDefFoundError) thrown);

      // قبل از crash چیزی روی stdout چاپ نمی‌شود
      String out = new String(baos.toByteArray(), StandardCharsets.UTF_8);
      assertTrue(out.trim().isEmpty());
  }

  @Test(timeout = 4000)
  public void test6_getCommandName_nonEmpty_constant() throws Throwable {
      ConsoleListFoldersCommand cmd = new ConsoleListFoldersCommand();
      String name = cmd.getCommandName();
      assertNotNull(name);
      assertFalse(name.isEmpty());
      assertEquals("lf", name);
  }

  // ==================== Added extra tests (each inherits the 0.5s delay via @Before) ====================

  @Test(timeout = 4000)
  public void test7_explain_outputHasNoNullLiteral() throws Throwable {
      final ConsoleListFoldersCommand cmd = new ConsoleListFoldersCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertFalse(out.toLowerCase().contains("null"));
      assertTrue(out.contains("(lf)"));
  }

  @Test(timeout = 4000)
  public void test8_explain_outputNotEmpty_andContainsKey() throws Throwable {
      final ConsoleListFoldersCommand cmd = new ConsoleListFoldersCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertTrue(out.trim().length() > 0);
      assertTrue(out.contains("command.list.folders.explain"));
  }

  @Test(timeout = 4000)
  public void test9_handleExecute_preservesCommandNameAfterFailure() throws Throwable {
      ConsoleListFoldersCommand cmd = new ConsoleListFoldersCommand();
      try {
          cmd.handleExecute();
          fail("Expecting NoClassDefFoundError");
      } catch (NoClassDefFoundError e) {
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
      // نام دستور باید همچنان ثابت باشد
      assertEquals("lf", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test10_handleExecute_twice_consistentExceptionType() throws Throwable {
      ConsoleListFoldersCommand cmd = new ConsoleListFoldersCommand();
      try {
          cmd.handleExecute();
          fail("Expecting NoClassDefFoundError (first call)");
      } catch (NoClassDefFoundError e) {
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
      try {
          cmd.handleExecute();
          fail("Expecting NoClassDefFoundError (second call)");
      } catch (NoClassDefFoundError e) {
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.core.FolderManager", e);
      }
  }

  @Test(timeout = 4000)
  public void test11_explain_unicodeSafe_outputStillContainsLf() throws Throwable {
      final ConsoleListFoldersCommand cmd = new ConsoleListFoldersCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      // صرفاً اطمینان از عدم کرش و حضور علامت دستور
      assertTrue(out.contains("(lf)"));
  }

  @Test(timeout = 4000)
  public void test12_explain_hasLineBreakOrClosingParen() throws Throwable {
      final ConsoleListFoldersCommand cmd = new ConsoleListFoldersCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      // پلتفرم ممکن است \n/‏\r\n متفاوت باشد؛ حداقل وجود پرانتز بسته در پیغام را چک می‌کنیم
      assertTrue(out.contains(")"));
  }
}
