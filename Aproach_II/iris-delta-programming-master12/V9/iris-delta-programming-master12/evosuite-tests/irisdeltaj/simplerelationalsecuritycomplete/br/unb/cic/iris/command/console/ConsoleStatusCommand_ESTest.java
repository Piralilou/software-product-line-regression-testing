/*
 * This file was automatically generated by EvoSuite
 * Fri Aug 29 22:24:22 GMT 2025
 */

package irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.command.console;

import org.junit.Test;
import java.io.ByteArrayOutputStream;
import java.io.PrintStream;
import java.nio.charset.StandardCharsets;

import static org.junit.Assert.*;
import static org.evosuite.runtime.EvoAssertions.*;
import irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.command.console.ConsoleStatusCommand;
import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.junit.runner.RunWith;
import org.junit.Before;

@RunWith(EvoRunner.class) 
@EvoRunnerParameters(
    mockJVMNonDeterminism = true, 
    useVFS = true, 
    useVNET = true, 
    resetStaticState = true, 
    separateClassLoader = true
) 
public class ConsoleStatusCommand_ESTest extends ConsoleStatusCommand_ESTest_scaffolding {

  // --- Per-test delay: 0.5 second (500 ms), safe with @Test(timeout = 4000)
  @Before
  public void halfSecondDelay() throws Exception {
      Thread.sleep(500L);
  }

  @Test(timeout = 4000)
  public void test0()  throws Throwable  {
      ConsoleStatusCommand consoleStatusCommand0 = new ConsoleStatusCommand();
      // Undeclared exception!
      try { 
        consoleStatusCommand0.handleExecute();
        fail("Expecting exception: NoClassDefFoundError");
      } catch(NoClassDefFoundError e) {
         //
         // javax/mail/search/SearchTerm
         //
         verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.command.console.ConsoleStatusCommand", e);
      }
  }

  @Test(timeout = 4000)
  public void test1()  throws Throwable  {
      ConsoleStatusCommand consoleStatusCommand0 = new ConsoleStatusCommand();
      String string0 = consoleStatusCommand0.getCommandName();
      assertEquals("status", string0);
  }

  @Test(timeout = 4000)
  public void test2()  throws Throwable  {
      ConsoleStatusCommand consoleStatusCommand0 = new ConsoleStatusCommand();
      consoleStatusCommand0.explain();
      assertEquals("status", consoleStatusCommand0.getCommandName());
  }

  /** helper: گرفتن stdout بدون لامبدا (سازگار با Java 8) */
  private String captureStdout(final Runnable r) {
      java.io.PrintStream orig = java.lang.System.out;
      java.io.ByteArrayOutputStream baos = new java.io.ByteArrayOutputStream();
      java.io.PrintStream ps = new java.io.PrintStream(baos);
      java.lang.System.setOut(ps);
      try {
          r.run();
      } finally {
          java.lang.System.setOut(orig);
          ps.flush();
          ps.close();
      }
      return new String(baos.toByteArray(), java.nio.charset.StandardCharsets.UTF_8);
  }

  @Test(timeout = 4000)
  public void test3_explain_printsUsageAndKey() throws Throwable {
      final ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertTrue(out.contains("(status)"));
      // اگر ResourceBundle موجود نباشد، خود کلید i18n چاپ می‌شود:
      assertTrue(out.contains("command.status.explain"));
      assertEquals("status", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test4_explain_multipleCalls_consistent() throws Throwable {
      final ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      String out1 = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      String out2 = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertTrue(out1.contains("(status)"));
      assertTrue(out2.contains("(status)"));
      assertTrue(out1.contains("command.status.explain"));
      assertTrue(out2.contains("command.status.explain"));
  }

  @Test(timeout = 4000)
  public void test5_handleExecute_throwsEarly_noStdoutPrinted() throws Throwable {
      final ConsoleStatusCommand cmd = new ConsoleStatusCommand();

      // گرفتن stdout و هم‌زمان انتظار exception
      java.io.PrintStream orig = java.lang.System.out;
      java.io.ByteArrayOutputStream baos = new java.io.ByteArrayOutputStream();
      java.io.PrintStream ps = new java.io.PrintStream(baos);
      java.lang.System.setOut(ps);

      Throwable thrown = null;
      try {
          cmd.handleExecute();
      } catch (Throwable t) {
          thrown = t;
      } finally {
          java.lang.System.setOut(orig);
          ps.flush();
          ps.close();
      }

      assertNotNull("Expected error due to missing mail infra", thrown);
      assertTrue(thrown instanceof NoClassDefFoundError);
      verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.command.console.ConsoleStatusCommand",
                      (NoClassDefFoundError) thrown);

      String out = new String(baos.toByteArray(), java.nio.charset.StandardCharsets.UTF_8);
      // قبل از کرش چیزی چاپ نشود (معمولاً status/provider باعث خطا می‌شود)
      assertTrue(out.trim().isEmpty());
  }

  @Test(timeout = 4000)
  public void test6_handleExecute_twice_failsConsistently() throws Throwable {
      ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      try {
          cmd.handleExecute();
          fail("Expected NoClassDefFoundError on first call");
      } catch (NoClassDefFoundError expected) {
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.command.console.ConsoleStatusCommand", expected);
      }
      try {
          cmd.handleExecute();
          fail("Expected NoClassDefFoundError on second call");
      } catch (NoClassDefFoundError expected) {
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.command.console.ConsoleStatusCommand", expected);
      }
  }

  @Test(timeout = 4000)
  public void test7_getCommandName_nonEmpty_andMatchesConstant() throws Throwable {
      ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      String name = cmd.getCommandName();
      assertNotNull(name);
      assertFalse(name.isEmpty());
      assertEquals(ConsoleStatusCommand.COMMAND_NAME, name);
  }

  @Test(timeout = 4000)
  public void test8_handleExecute_doesNotPrintProviderLinesOnFailure() throws Throwable {
      final ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      String out = captureStdout(new Runnable() {
          @Override public void run() {
              try { cmd.handleExecute(); } catch (Throwable ignored) { }
          }
      });
      // در صورت شکست زودهنگام، نباید خطوط Provider/Username چاپ شود
      assertFalse(out.contains(" - Provider: "));
      assertFalse(out.contains(" - Username: "));
  }

  @Test(timeout = 4000)
  public void test9_multipleInstances_sameBehavior() throws Throwable {
      ConsoleStatusCommand a = new ConsoleStatusCommand();
      ConsoleStatusCommand b = new ConsoleStatusCommand();
      assertEquals("status", a.getCommandName());
      assertEquals("status", b.getCommandName());
  }

  // ================= Added extra tests (inherit 0.5s delay via @Before) =================

  @Test(timeout = 4000)
  public void test10_explain_outputHasNoNullLiteral() throws Throwable {
      final ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertFalse(out.toLowerCase().contains("null"));
      assertTrue(out.contains("(status)"));
  }

  @Test(timeout = 4000)
  public void test11_explain_consistentAcrossTwoCalls_equalStrings() throws Throwable {
      final ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      String a = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } }).trim();
      String b = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } }).trim();
      assertFalse(a.isEmpty());
      assertEquals(a, b);
  }

  @Test(timeout = 4000)
  public void test12_handleExecute_preservesCommandNameAfterFailure() throws Throwable {
      ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      try {
          cmd.handleExecute();
          fail("Expected NoClassDefFoundError");
      } catch (NoClassDefFoundError e) {
          verifyException("irisdeltaj.simplerelational.br.unb.cic.iris.command.console.ConsoleStatusCommand", e);
      }
      assertEquals("status", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test13_getCommandName_idempotentAcrossCalls() throws Throwable {
      ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      String n1 = cmd.getCommandName();
      String n2 = cmd.getCommandName();
      String n3 = cmd.getCommandName();
      assertEquals("status", n1);
      assertEquals(n1, n2);
      assertEquals(n2, n3);
  }

  @Test(timeout = 4000)
  public void test14_explain_containsParenthesesAndKeyOrText() throws Throwable {
      final ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertTrue(out.contains("(") && out.contains(")"));
      // یا کلید i18n یا توضیح متنی
      assertTrue(out.contains("command.status.explain") || out.toLowerCase().contains("status"));
  }

  @Test(timeout = 4000)
  public void test15_handleExecute_noCheckedExceptionLeaks() throws Throwable {
      ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      try {
          cmd.handleExecute();
          fail("Expected NoClassDefFoundError");
      } catch (NoClassDefFoundError e) {
          // OK: unchecked only
      }
  }

  @Test(timeout = 4000)
  public void test16_explain_unicodeSafe_noCrash() throws Throwable {
      final ConsoleStatusCommand cmd = new ConsoleStatusCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      // چاپ ساده و بدون استثناء
      assertTrue(out.contains("(status)"));
  }
}
