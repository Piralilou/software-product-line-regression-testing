/*
 * This file was automatically generated by EvoSuite
 * Fri Aug 29 22:28:39 GMT 2025
 */

package irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.command.console;

import org.junit.Test;
import java.io.ByteArrayOutputStream;
import java.io.PrintStream;
import java.nio.charset.StandardCharsets;

import static org.junit.Assert.*;
import irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.command.console.CurrentFolderConsoleCommand;
import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.junit.runner.RunWith;
import org.junit.Before;

@RunWith(EvoRunner.class) @EvoRunnerParameters(mockJVMNonDeterminism = true, useVFS = true, useVNET = true, resetStaticState = true, separateClassLoader = true) 
public class CurrentFolderConsoleCommand_ESTest extends CurrentFolderConsoleCommand_ESTest_scaffolding {

  /** تاخیر نیم‌ثانیه‌ای قبل از اجرای هر تست */
  @Before
  public void delay() throws Exception {
      Thread.sleep(500);
  }

  @Test(timeout = 4000)
  public void test0()  throws Throwable  {
      CurrentFolderConsoleCommand currentFolderConsoleCommand0 = new CurrentFolderConsoleCommand();
      currentFolderConsoleCommand0.explain();
      assertEquals("pwd", currentFolderConsoleCommand0.getCommandName());
  }

  @Test(timeout = 4000)
  public void test1()  throws Throwable  {
      CurrentFolderConsoleCommand currentFolderConsoleCommand0 = new CurrentFolderConsoleCommand();
      currentFolderConsoleCommand0.handleExecute();
      assertEquals("pwd", currentFolderConsoleCommand0.getCommandName());
  }

  @Test(timeout = 4000)
  public void test2()  throws Throwable  {
      CurrentFolderConsoleCommand currentFolderConsoleCommand0 = new CurrentFolderConsoleCommand();
      String string0 = currentFolderConsoleCommand0.getCommandName();
      assertEquals("pwd", string0);
  }

  /** helper: گرفتن stdout بدون لامبدا و با java.lang.System تا با EvoSuite System تداخل نکند */
  private String captureStdout(final Runnable r) {
      PrintStream orig = java.lang.System.out;
      ByteArrayOutputStream baos = new ByteArrayOutputStream();
      PrintStream ps = new PrintStream(baos);
      java.lang.System.setOut(ps);
      try {
          r.run();
      } finally {
          java.lang.System.setOut(orig);
          ps.flush();
          ps.close();
      }
      return new String(baos.toByteArray(), StandardCharsets.UTF_8);
  }

  @Test(timeout = 4000)
  public void test3_explain_printsExactMessage() throws Throwable {
      final CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      // متن دقیقِ متد explain (توجه: پرانتز اضافی پایانی در کلاس وجود دارد)
      assertTrue(out.contains("(pwd) - show current folder)"));
      assertEquals("pwd", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test4_handleExecute_capturesStdout_noCrashLeak() throws Throwable {
      final CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      String out = captureStdout(new Runnable() {
          @Override public void run() {
              try {
                  cmd.handleExecute();
              } catch (Throwable ignored) {
                  // در نبود زیرساخت FolderManager ممکن است خطا بیاید؛ نگذاریم تست fail شود
              }
          }
      });
      assertNotNull(out);
      // اگر زیرساخت موجود باشد، باید نام پوشه چاپ شود؛ در غیر این‌صورت هم تست نباید fail شود
  }

  @Test(timeout = 4000)
  public void test5_handleExecute_twice_idempotentBehavior() throws Throwable {
      final CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      try { cmd.handleExecute(); } catch (Throwable ignored) {}
      try { cmd.handleExecute(); } catch (Throwable ignored) {}
      assertEquals("pwd", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test6_getCommandName_matchesConstant_andNonEmpty() throws Throwable {
      CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      String name = cmd.getCommandName();
      assertNotNull(name);
      assertFalse(name.isEmpty());
      assertEquals(CurrentFolderConsoleCommand.COMMAND_NAME, name);
  }

  @Test(timeout = 4000)
  public void test7_explain_multipleCalls_consistentOutput() throws Throwable {
      final CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      String out1 = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      String out2 = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertTrue(out1.contains("(pwd) - show current folder)"));
      assertTrue(out2.contains("(pwd) - show current folder)"));
  }

  @Test(timeout = 4000)
  public void test8_handleExecute_stdoutDoesNotContainExceptionText() throws Throwable {
      final CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      String out = captureStdout(new Runnable() {
          @Override public void run() {
              try { cmd.handleExecute(); } catch (Throwable ignored) {}
          }
      });
      // هر گونه استک‌تریس احتمالی باید روی stderr برود؛ stdout نباید متن Exception داشته باشد
      assertFalse(out.contains("Exception"));
  }

  // -----------------------
  // تست‌های اضافه‌شده
  // -----------------------

  @Test(timeout = 4000)
  public void test9_explain_startsWithParenCommandAndDash() throws Throwable {
      final CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertTrue("Explain باید با \"(pwd) - \" شروع شود",
              out.trim().startsWith("(pwd) - "));
  }

  @Test(timeout = 4000)
  public void test10_commandName_trim_lowercase_noSpaces() throws Throwable {
      CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      String name = cmd.getCommandName();
      assertEquals("pwd", name);
      assertEquals(name, name.trim());
      assertEquals("pwd", name.toLowerCase());
      assertFalse("نام دستور نباید فاصله داشته باشد", name.contains(" "));
  }

  @Test(timeout = 4000)
  public void test11_explain_twoInstances_sameOutput() throws Throwable {
      final CurrentFolderConsoleCommand a = new CurrentFolderConsoleCommand();
      final CurrentFolderConsoleCommand b = new CurrentFolderConsoleCommand();
      String out1 = captureStdout(new Runnable() { @Override public void run() { a.explain(); } });
      String out2 = captureStdout(new Runnable() { @Override public void run() { b.explain(); } });
      assertEquals(out1.trim(), out2.trim());
  }

  @Test(timeout = 4000)
  public void test12_handleExecute_threeTimes_noStateLeak() throws Throwable {
      final CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      try { cmd.handleExecute(); } catch (Throwable ignored) {}
      try { cmd.handleExecute(); } catch (Throwable ignored) {}
      try { cmd.handleExecute(); } catch (Throwable ignored) {}
      assertEquals("pwd", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test13_explain_containsPwdOnce() throws Throwable {
      final CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      int first = out.indexOf("(pwd)");
      int last  = out.lastIndexOf("(pwd)");
      assertTrue(first >= 0);
      assertEquals("label (pwd) باید فقط یک بار در explain بیاید", first, last);
  }

  @Test(timeout = 4000)
  public void test14_handleExecute_stdoutNoStackTracePatterns() throws Throwable {
      final CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { try { cmd.handleExecute(); } catch (Throwable ignored) {} }});
      // الگوهای رایج استک‌تریس نباید در stdout باشند
      assertFalse(out.contains("at "));
      assertFalse(out.toLowerCase().contains("error"));
  }

  @Test(timeout = 4000)
  public void test15_explain_then_handleExecute_stdoutCaptured() throws Throwable {
      final CurrentFolderConsoleCommand cmd = new CurrentFolderConsoleCommand();
      String out1 = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      String out2 = captureStdout(new Runnable() { @Override public void run() { try { cmd.handleExecute(); } catch (Throwable ignored) {} }});
      assertNotNull(out1);
      assertNotNull(out2);
      assertTrue(out1.contains("(pwd)"));
  }

}
