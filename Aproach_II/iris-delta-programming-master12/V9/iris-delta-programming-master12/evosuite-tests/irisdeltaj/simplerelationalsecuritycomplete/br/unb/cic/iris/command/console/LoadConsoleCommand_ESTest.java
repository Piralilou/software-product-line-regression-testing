/*
 * This file was automatically generated by EvoSuite
 * Fri Aug 29 22:39:50 GMT 2025
 */

package irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.command.console;

import org.junit.Test;
import java.io.ByteArrayOutputStream;
import java.io.PrintStream;
import java.nio.charset.StandardCharsets;

import static org.junit.Assert.*;
import static org.evosuite.runtime.EvoAssertions.*;
import irisdeltaj.simplerelationalsecuritycomplete.br.unb.cic.iris.command.console.LoadConsoleCommand;
import java.util.NoSuchElementException;
import org.evosuite.runtime.EvoRunner;
import org.evosuite.runtime.EvoRunnerParameters;
import org.evosuite.runtime.util.SystemInUtil;
import org.junit.runner.RunWith;
import org.junit.Before;

@RunWith(EvoRunner.class) 
@EvoRunnerParameters(
    mockJVMNonDeterminism = true, 
    useVFS = true, 
    useVNET = true, 
    resetStaticState = true, 
    separateClassLoader = true
) 
public class LoadConsoleCommand_ESTest extends LoadConsoleCommand_ESTest_scaffolding {

  // --- Per-test delay: 0.5 second (500 ms), safe with @Test(timeout = 4000)
  @Before
  public void halfSecondDelay() throws Exception {
      Thread.sleep(500L);
  }

  @Test(timeout = 4000)
  public void test0()  throws Throwable  {
      LoadConsoleCommand loadConsoleCommand0 = new LoadConsoleCommand();
      SystemInUtil.addInputLine("load");
      loadConsoleCommand0.handleExecute();
      assertEquals("load", loadConsoleCommand0.getCommandName());
  }

  @Test(timeout = 4000)
  public void test1()  throws Throwable  {
      LoadConsoleCommand loadConsoleCommand0 = new LoadConsoleCommand();
      loadConsoleCommand0.explain();
      assertEquals("load", loadConsoleCommand0.getCommandName());
  }

  @Test(timeout = 4000)
  public void test2()  throws Throwable  {
      LoadConsoleCommand loadConsoleCommand0 = new LoadConsoleCommand();
      String string0 = loadConsoleCommand0.getCommandName();
      assertEquals("load", string0);
  }

  @Test(timeout = 4000)
  public void test3()  throws Throwable  {
      LoadConsoleCommand loadConsoleCommand0 = new LoadConsoleCommand();
      // Undeclared exception!
      try { 
        loadConsoleCommand0.handleExecute();
        fail("Expecting exception: NoSuchElementException");
      } catch(NoSuchElementException e) {
         //
         // No line found
         //
         verifyException("java.util.Scanner", e);
      }
  }

  /** helper: گرفتن stdout با java.lang.System (برای اجتناب از تداخل با org.evosuite.runtime.System) */
  private String captureStdout(final Runnable r) {
      PrintStream orig = java.lang.System.out;
      ByteArrayOutputStream baos = new ByteArrayOutputStream();
      PrintStream ps = new PrintStream(baos);
      java.lang.System.setOut(ps);
      try {
          r.run();
      } finally {
          java.lang.System.setOut(orig);
          ps.flush();
          ps.close();
      }
      return new String(baos.toByteArray(), StandardCharsets.UTF_8);
  }

  @Test(timeout = 4000)
  public void test4_explain_printsUsageAndText() throws Throwable {
      final LoadConsoleCommand cmd = new LoadConsoleCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertTrue(out.contains("(load)"));
      assertTrue(out.contains("Testando carga dinamica de command"));
      assertEquals("load", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test5_handleExecute_withEmptyInput_printsPromptAndClasspath_noThrow() throws Throwable {
      // ورودی خالی؛ متد باید prompt و حداقل یک بار classpath را چاپ کند و استثناء به بیرون ندهد (استثناء‌ها catch می‌شوند)
      org.evosuite.runtime.util.SystemInUtil.addInputLine("");
      final LoadConsoleCommand cmd = new LoadConsoleCommand();

      String out = captureStdout(new Runnable() { @Override public void run() { cmd.handleExecute(); } });
      assertNotNull(out);
      assertTrue(out.contains("Jar file path"));
      assertTrue(out.contains("classpath=")); // showClassPath قبل از خواندن ورودی اجرا می‌شود
  }

  @Test(timeout = 4000)
  public void test6_handleExecute_withCustomPath_printsPromptAndClasspath() throws Throwable {
      // مسیر جعلی؛ صرفاً بررسی می‌کنیم چاپ‌ها انجام می‌شوند و متد کرش نمی‌کند (استثناء‌ها داخل خودش چاپ می‌شوند)
      org.evosuite.runtime.util.SystemInUtil.addInputLine("C:/fake/path/libFoo.jar");
      final LoadConsoleCommand cmd = new LoadConsoleCommand();

      String out = captureStdout(new Runnable() { @Override public void run() { cmd.handleExecute(); } });
      assertNotNull(out);
      assertTrue(out.contains("Jar file path"));
      assertTrue(out.contains("classpath="));
      // ممکن است به خاطر شکست cast به URLClassLoader یا loadClass، فقط یک بار classpath چاپ شود؛ کافی است موجود باشد.
  }

  @Test(timeout = 4000)
  public void test7_handleExecute_multipleRuns_differentInputs_noCrash() throws Throwable {
      final LoadConsoleCommand cmd = new LoadConsoleCommand();

      org.evosuite.runtime.util.SystemInUtil.addInputLine("/tmp/a.jar");
      String out1 = captureStdout(new Runnable() { @Override public void run() { cmd.handleExecute(); } });
      assertNotNull(out1);
      assertTrue(out1.contains("classpath="));

      org.evosuite.runtime.util.SystemInUtil.addInputLine("/tmp/b.jar");
      String out2 = captureStdout(new Runnable() { @Override public void run() { cmd.handleExecute(); } });
      assertNotNull(out2);
      assertTrue(out2.contains("classpath="));

      assertEquals("load", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test8_getCommandName_nonEmpty_matchesConstant() throws Throwable {
      LoadConsoleCommand cmd = new LoadConsoleCommand();
      String name = cmd.getCommandName();
      assertNotNull(name);
      assertFalse(name.isEmpty());
      assertEquals(LoadConsoleCommand.COMMAND_LOAD, name);
  }

  // ================= Added extra tests (inherit 0.5s delay via @Before) =================

  @Test(timeout = 4000)
  public void test9_explain_outputDoesNotContainNullLiteral() throws Throwable {
      final LoadConsoleCommand cmd = new LoadConsoleCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertTrue(out.contains("(load)"));
      assertFalse(out.toLowerCase().contains("null"));
  }

  @Test(timeout = 4000)
  public void test10_explain_consistentAcrossTwoCalls_equalStrings() throws Throwable {
      final LoadConsoleCommand cmd = new LoadConsoleCommand();
      String a = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } }).trim();
      String b = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } }).trim();
      assertFalse(a.isEmpty());
      assertEquals(a, b);
  }

  @Test(timeout = 4000)
  public void test11_handleExecute_withWindowsStylePath_printsClasspath() throws Throwable {
      final LoadConsoleCommand cmd = new LoadConsoleCommand();
      SystemInUtil.addInputLine("C:\\temp\\foo.jar");
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.handleExecute(); } });
      assertTrue(out.contains("Jar file path"));
      assertTrue(out.contains("classpath="));
  }

  @Test(timeout = 4000)
  public void test12_handleExecute_withWhitespaceInput_printsPrompt() throws Throwable {
      final LoadConsoleCommand cmd = new LoadConsoleCommand();
      SystemInUtil.addInputLine("   ");
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.handleExecute(); } });
      assertTrue(out.contains("Jar file path"));
      assertTrue(out.contains("classpath="));
  }

  @Test(timeout = 4000)
  public void test13_handleExecute_thenGetCommandName_stable() throws Throwable {
      final LoadConsoleCommand cmd = new LoadConsoleCommand();
      SystemInUtil.addInputLine("/opt/lib/test.jar");
      captureStdout(new Runnable() { @Override public void run() { cmd.handleExecute(); } });
      assertEquals("load", cmd.getCommandName());
  }

  @Test(timeout = 4000)
  public void test14_multipleInstances_sameCommandName() throws Throwable {
      LoadConsoleCommand a = new LoadConsoleCommand();
      LoadConsoleCommand b = new LoadConsoleCommand();
      assertEquals("load", a.getCommandName());
      assertEquals(a.getCommandName(), b.getCommandName());
  }

  @Test(timeout = 4000)
  public void test15_handleExecute_noInput_thenInput_inTwoCalls() throws Throwable {
      final LoadConsoleCommand cmd = new LoadConsoleCommand();

      // بار اول بدون ورودی → NoSuchElementException
      boolean threw = false;
      try {
          captureStdout(new Runnable() { @Override public void run() { cmd.handleExecute(); } });
      } catch (NoSuchElementException expected) {
          threw = true;
      }
      assertTrue(threw);

      // بار دوم با ورودی معتبرِ ساختگی
      SystemInUtil.addInputLine("/some/path.jar");
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.handleExecute(); } });
      assertTrue(out.contains("Jar file path"));
  }

  @Test(timeout = 4000)
  public void test16_explain_containsParenthesesAndKeyword() throws Throwable {
      final LoadConsoleCommand cmd = new LoadConsoleCommand();
      String out = captureStdout(new Runnable() { @Override public void run() { cmd.explain(); } });
      assertTrue(out.contains("(load)"));
      // متن i18n یا متن پیش‌فرض
      assertTrue(out.contains("Testando") || out.toLowerCase().contains("load"));
  }
}
